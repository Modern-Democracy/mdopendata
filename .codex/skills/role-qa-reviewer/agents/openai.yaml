interface:
  display_name: "QA Reviewer"
  short_description: "Verify changes with evidence"
  default_prompt: "Use $role-qa-reviewer to verify the completed work against the acceptance claim with discriminating evidence."
