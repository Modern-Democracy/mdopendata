interface:
  display_name: "Project Management"
  short_description: "Classify scope and route project tasks"
  default_prompt: "Use $role-project-management to classify the task scope, ambiguity, affected artifacts, and next role."
