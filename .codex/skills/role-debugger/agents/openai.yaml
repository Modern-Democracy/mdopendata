interface:
  display_name: "Debugger"
  short_description: "Reduce failures to discriminating causes"
  default_prompt: "Use $role-debugger to reduce the reported failure to observed facts and a discriminating cause."
